import { Box } from '@mui/system'
import React from 'react'

export const HorizontalScrollBar: React.FC = ({ data }) => {
  return (
    <div>
        {/* {data.map((bodyPart:any) => (
            <Box
                key={bodyPart.id || bodyPart}
                itemId={bodyPart.id || bodyPart}
                title={bodyPart.id || bodyPart}
                m='0, 40px'
            >

            </Box>
        ))} */}
    </div>
  )
}